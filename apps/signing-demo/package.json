{
  "name": "@autopen/signing-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/bundle-client.mjs",
    "typecheck": "tsc -p tsconfig.typecheck.json && tsc -p tsconfig.web.json",
    "test": "vitest run --passWithNoTests",
    "start": "node --env-file-if-exists=../../.env dist/server.js"
  },
  "dependencies": {
    "@autopen/adapter-lakaut": "workspace:*",
    "@autopen/core": "workspace:*",
    "@t3-oss/env-core": "0.13.11",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@autopen/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "esbuild": "0.28.2",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
