{
  "name": "@autopen/webhook-receiver",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "test": "vitest run --passWithNoTests",
    "start": "node --env-file=../../.env dist/main.js"
  },
  "devDependencies": {
    "@types/node": "22.18.1",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
